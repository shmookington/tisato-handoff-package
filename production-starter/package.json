{
  "name": "tisato-production-starter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Runnable booking/admin/email starter for TISATO Transportation Services.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "hash-password": "node scripts/hash-password.js",
    "check": "find src scripts public/assets/js -name '*.js' -print0 | xargs -0 -n 1 node --check && node --check server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
